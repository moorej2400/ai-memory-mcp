# Claude Instructions

Read [AGENTS.md](AGENTS.md) first. It has the mandatory constraints for this repository.

## Local operator notes

Read `local.agents.md` at the repository root when the file exists.

This repository is public. `local.agents.md` holds the machine names, account
names, paths, and test-environment procedures that a public repository must not
contain. Git ignores the file.

If `local.agents.md` does not exist, continue without it. Do not create it from
guesswork, and do not copy its content into a file that Git can commit.
